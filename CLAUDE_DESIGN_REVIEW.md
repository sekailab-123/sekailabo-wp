# SEKAILABO' コーポレートサイト デザイン/UX監査

> Claude Codeによるレビュー。ソースコードの編集は行わず、現行実装を読み取り監査した結果。

## 0. 総括

現状は「エディトリアルな夜（Hero/Showcase/Topics＝暗色・動画駆動）」と「フラットな昼（About/Business/Info＝暖色クリーム）」が接続なしに交互する構成。クラターの主因は装飾の量ではなく、**固定背景動画がヒーロー以外の複数セクションにも透過し続けていること**と、**縦動画を横長ビューポートに `object-fit: cover` で強制フィットさせていること**の2点に集約できる。

## 1. 原因診断

### 1-1. 縦動画ヒーローが「間延び・低画質」に見える直接原因

- `bg_mv.mp4`は実測 **1080×1920（9:16）、H.264、約4.5Mbps、55MB**（Git LFS管理）。
- `#bgvid { object-fit: cover }` が `css/front-page.css:77-81` と `css/basic.css:1874-1878` に重複定義。コンテナ `.mainback` は `position: fixed` で常に画面全面（`front-page.css:39-47`、`basic.css:1857-1865`）。
- デスクトップ1920×1080では、動画の幅を1080→1920pxへ約178%アップスケールするため、低画質感が出る。高さは3413px相当になり、画面に見えるのは中央部だけとなる。縦動画の文脈が失われた中途半端な帯表示が「間延び」の正体。
- モバイルでは動画とビューポートの縦横比が近く、問題は主にデスクトップ特有。
- ヒーローは `#mainvisual::before/::after`（`front-page.css:200-234`）と `#main_title::before`（255-269）の3層のblurビネットで可読性を補っており、対症療法かつ描画コスト増になっている。

### 1-2. 背景と情報の混在

- `body.home{background:transparent!important}`（`front-page.css:10-13`）に加え、Showcase（517-520）・Topics（762-765）は背景未指定。そのため固定動画がヒーローだけでなく複数セクションに透過している。
- Showcaseでは動く背景に静止画カードと低コントラスト文字が重なり、動画・静止画・文字の3層が競合している。
- `section-eyebrow`連番のopacityが0.05〜0.08（`front-page.css:391,503`）と極薄で、動く背景・半透明カードとの組み合わせがノイズになっている。
- 「夜」と「昼」のセクション群が接続要素なしに交互し、情報設計全体も未整理に見える。

### 1-3. レガシーCSS/JSの残骸

- `basic.css`（2447行）・`sp.css`（2873行）に旧ヒーロー実装や、現行DOMにない旧コンポーネント用セレクタが残る。
- `sp.css:80`の`background-image:url(back.jpg)`は動画に隠れ、モバイル全ページで不要な188KBを読み込む。
- `js/ui.js`は存在しないIDへのバインドを毎回実行している。
- `sp.css:93`の`#main_title{height:210px}`が現行ヒーローに残っている。
- `.mainback`が`basic.css`と`front-page.css`に重複定義され、読み込み順に依存する脆い状態。

### 1-4. メタ情報の不整合

- `header.php:24-41`のmeta description/keywords/OGPは旧ポジショニングのままで、現行のブランドストーリーと不一致。
- `og:image`も空。新しいコピーはユーザー確認後に決めるべき範囲。

## 2. アートディレクション

- **視覚階層**：1ビューにつき主役の画像・映像レイヤーは1つまで。
- **タイポグラフィ**：Cormorant Garamond（見出し）＋Noto Sans JP（本文）の2軸。Ralewayは微小UIラベル専用に限定。
- **カラー**：「夜」と「昼」のコントラストは残しつつ、CTA・ホバー・連番を結ぶシグネチャーアクセントカラーを一貫使用。
- **余白**：現行の120〜140px程度の余白量は活かし、減らすのではなく「重ねない」設計で整理する。
- **モーション**：動画はヒーローまたは専用リールセクションに限定。その他は不透明背景にする。
- **画像・動画ルール**：背景動画はコンテナ比率に合わせて意図的にクロップして書き出すか、ネイティブ比率のまま額装する。比率が違うまま`object-fit: cover`を機械的に当てない。

## 3. ヒーロー戦略

### 案A：アンビエント背景＋前景メディア

動画を強めのブラー・拡大・減光でアンビエント層にし、その上にネイティブ9:16のまま前景パネルを置く。デスクトップではコピーの横に幅420〜480px程度で右寄せ、モバイルではほぼ全幅へ拡張する。

- 長所：映像主導の没入感を維持できる
- 短所：実装がやや複雑

### 案B：静的アートディレクションヒーロー＋動画を下層へ移設（推奨）

ヒーローは、動画から書き出した意図的にクロップ済みの静止画などを使い、動画本体はShowcaseを転用した専用リールセクションでネイティブ9:16のまま見せる。既存の`showcase-slide`（`front-page.css:579-608`）は縦長コンテナの基盤として流用可能。スクロールイン時のみ再生し、画面外では停止する。

- 長所：問題を根本回避でき、LCPが安定し、実装コストも低い
- 短所：初見の「動いている感」は薄くなる

**Claudeの推奨は案B。** 縦動画を無理に背景へ合わせず、コンテンツとして正しい比率で見せるほうが、ブランドの世界観とサイト性能を両立できる。

## 4. 推奨情報設計

現状：Hero → About → Showcase → Business → Topics → Info → CTA

推奨：

1. Hero
2. About
3. **Business**
4. Reel / Showcase
5. Topics
6. About Us / Contact

Showcaseに仮テキストが残っているため、実コンテンツが整うまでは非表示も選択肢。訪問者には雰囲気画像より先に具体的な事業内容を示す。

## 5. 優先度別実装計画

### Quick wins

1. `sp.css:93`のheight漏れを修正
2. `sp.css:80`の未使用`back.jpg`読み込みを削除
3. Showcaseの仮テキストを差し替え、または一時非表示
4. `header.php`のOGP・metaを現行ブランドに整合（コピーは確認後）
5. Showcase / Topicsに不透明背景を追加して動画の透過を遮断
6. Font Awesomeフルキットを必要なインラインSVGへ置換
7. `ui.js`の死んだバインドを整理

### Core redesign

1. 推奨の案B、または案Aでヒーローを再構築
2. `bg_mv.mp4`をリール用途に合わせて再エンコード
3. `.mainback`のfixedスコープをヒーロー内に限定
4. IAを上記の順へ並び替え
5. 「夜」と「昼」をつなぐ共通モチーフ導入
6. `basic.css` / `sp.css`の死んだセレクタを全テンプレート確認後に整理
7. `.mainback`の重複定義を統合

### Optional polish

1. グレイン・テクスチャで共通モチーフを強化
2. `section-eyebrow`のopacityをトークン化
3. WebM / AV1ソース追加
4. Businessリストのスクロール連動アニメーション

## 6. 実装上の注意

- **レスポンシブ**：`header.php:101-110`の768/769px境界でのCSS丸ごと出し分けは脆い。320 / 375 / 768 / 1024 / 1440pxで確認が必要。
- **Reduced motion**：動画・スクロール演出に`prefers-reduced-motion`分岐がない。静止ポスター切替とアニメーション無効化を入れる。
- **パフォーマンス**：55MB動画も背景ループとしては過大。目安3〜5MBまで圧縮する。`preload="none"`と可視時再生を推奨。`backdrop-filter`と複数の`blur()`同時稼働も削減対象。
- **WordPress保守性**：共有CSSの削除前には全テンプレート横断確認が必須。`front-page.css`の`!important`多用は、グローバルCSSとの競合を整理して減らす。
- **LFS**：GitHub Actions + FTPがLFSポインタでなく実バイナリを配信できているか、本番で動画表示の一次確認を推奨。

## 7. アセット・コンテンツの扱い

- **保持**：`front-page.php`の本文コピー、`mogs.png`、Showcaseカード画像（`editorial.png` / `motion.png` / `showcase.png`）
- **差し替え**：Showcaseの仮テキスト、`header.php`のmeta description / keywords / OGP
- **レガシーとして要確認**：`basic.css` / `sp.css`内の旧ヒーロー実装と対応`ui.js`、`front-page.php`から未参照の旧アセット。削除判断は全テンプレート横断確認後に別途行う。
